let word = "JavaScript";

// Split the word into an array of characters, reverse the array, then join back to string
let reversedWord = word.split('').reverse().join('');

console.log(`Original: ${word}, Reversed: ${reversedWord}`);